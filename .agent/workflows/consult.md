---
description: Use this when you want code patterns/logic without file modifications.
---

// turbo-all

1. Use `view_file` to read the context if specific files are mentioned.
2. Provide the full code solution in the chat.
3. **CONSTRAINT**: DO NOT call any tool that modifies files (write, replace, delete).
4. **CONSTRAINT**: DO NOT call terminal commands that commit or push code.
5. Explain the steps the user should take to implement the code themselves.
